# 本地 LLM 自动化回归记录：2026-09-12

## 验证对象与范围

本记录归档一次已完成的本机回归，日期按 America/Los_Angeles 时区。
操作入口和长期测试合同见[本地 LLM 指南](../local-llm.md)；返回[文档索引](../README.md)。

- Harness 基线提交：`21cc6350e03d430f1d4ce2a581c147505d66fa43`。
- 待发布增量：本地模型测试、Modelfile、模型 lock、前台启动脚本和说明文档。
- `src/`、Cargo 依赖与原有 CI 未变；测试和配置的内容指纹列在下节。
- 这轮回归之后仅整理测试文档和本记录；记录不以“最新 master”作为不可变验证对象。
- 真实模型用例的文件副作用发生在测试创建的临时目录，这些用例无 shell 授权、付费 API
  或远端数据写入；确定性回归另含受控 shell 执行与取消测试。

## 环境与不可变输入

| 项目 | 实测环境 |
|---|---|
| 系统 / 硬件 | macOS 15.7.3 / Apple M4 Pro / arm64 / 48 GiB 内存 |
| Rust | rustc 1.98.1，按仓库 `rust-toolchain.toml` |
| Runtime | Ollama 0.33.3，官方 standalone bundle |
| 测试模型 | `turnforge-test:qwen3-4b-v1`，Qwen3-4B-Instruct-2507，Q4_K_M |
| API / 监听 | `http://127.0.0.1:11434/v1`，仅 IPv4 loopback |
| 推理参数 | context 8192、num_predict 1024、temperature 0、seed 42；串行请求 |

服务版本和完整测试模型 digest 在每个真实用例前通过 API 与
[lock](../../dev/local-llm.lock.json) 比较；digest 为
`1cfb1620611f6fdcdbc90e3c32f0b0123b7ac36502c790eb90897941703ec363`。
`/api/ps` 另外确认实际 context 为 8192；模型加载占用约 3.86 GB，不能当作所有负载的上限。

下列 SHA-256 对应实际执行的新增测试和服务输入，验证前后保持一致：

```text
70adee64f0a683fff3d5838537f6c4a34cbb8b1a032d6de5780eb371a61560e6  tests/local_llm.rs
a5c79f44a59991b1371973068a1e35fef7b4c751b76ea6fe522ac2f826ea219c  dev/Modelfile.local
510fe2ffcbb2ee79f6bb5daa78cc2bf238c8c80353148bfb60bc102e0d685202  dev/local-llm.lock.json
714ba3bf583a7e3cc3b00263b41631bb5b1c3b31ea51c837e3f4b8960fba529e  scripts/serve-local-llm.sh
```

在仓库根目录用 `shasum -a 256` 检查这四个相对路径即可复核。文档自身不属于上述内容指纹。
完整模型 digest、安装包 URL/SHA 与源模型溯源信息以该版本 lock 为准。

## 已执行检查

以下均首次通过，没有失败后反复重跑到绿：

| 命令（仓库根目录） | 结果 |
|---|---|
| `cargo fmt --all -- --check` | 通过 |
| `cargo clippy --locked --all-targets -- -D warnings` | 通过 |
| `cargo test --locked --all-targets` | 28 通过；3 个真实模型用例按设计 ignored |
| `cargo test --locked --release --all-targets` | 28 通过；3 个真实模型用例按设计 ignored |
| `cargo build --locked --release` | 通过 |
| `cargo test --locked --test local_llm -- --ignored --test-threads=1 --nocapture` | 3/3 通过，总计 6.11 秒，含本次服务启动后的首次模型加载 |
| `cargo test --locked --release --test local_llm -- --ignored --test-threads=1 --nocapture` | 独立验证执行，3/3 通过，总计 3.86 秒 |
| `bash -n scripts/serve-local-llm.sh`、`git diff --check` | 通过 |

确定性回归由独立验证者运行；真实模型 debug / release 分别执行，未同时争抢模型服务。
默认回归中的 ignored 不计入通过数；真实用例通过证据来自两条显式 `--ignored` 命令。

| 用例 | Debug | Release | 本轮可观察结果 |
|---|---|---|---|
| LLM-001 文本流 | 通过，2.98 秒 | 通过，0.72 秒 | 无工具调用，有非空流式文本、最终回答和 completed |
| LLM-002 读文件 | 通过，1.59 秒 | 通过，1.60 秒 | 各有一次 `list_files`、一次 `read_file`；工具结果和回答包含未知 marker |
| LLM-003 写文件 | 通过，1.53 秒 | 通过，1.54 秒 | 各有一次 `write_file`；独立磁盘内容、成功结果与事件闭环符合预期 |

耗时取自测试输出并四舍五入，包含基线检查和 CLI 运行；不是纯推理速度基准。
工具次数只是本轮观测，不是要求后续每轮固定次数或调用顺序的 golden。

## 问题与结论

运行前发现前台 Ollama 服务未运行，既有模型缓存和 runtime 完整。通过已有启动脚本恢复，
未重新下载模型、修改 lock、安装开机启动或暴露局域网端口；这是测试前置条件恢复，不是代码修复。
本轮未复现需要修改 Harness 或测试实现的缺陷。

- 静态/构建证据与确定性回归通过。
- 真实本机 API → SSE → Turnforge CLI → 工具 → 模型继续回答的三个受控场景通过。
- 未验证真实模型在 Linux/其它硬件上的行为、复杂 coding 任务、长期稳定性或负载上限。
- 本记录不声称远端 CI 已通过；发布后的 PR/master CI 状态应查看对应 GitHub Actions run。
  默认远端 CI 不下载模型，也不执行这三个显式忽略的真实模型用例。

因此结论是：上述冻结代码与模型基线在本机完成了本轮回归，不是跨平台或生产就绪承诺。
